{
  "branches": ["main"],
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    [
      "@semantic-release/changelog",
      {
        "changelogFile": "CHANGELOG.md",
        "changelogTitle": "# Changelog\n\nИстория до отделения от апстрима (версии 3.0.0–5.0.0, проект\n`theYahia/yandex-direct-mcp`) — в\n[docs/CHANGELOG-upstream.md](docs/CHANGELOG-upstream.md)."
      }
    ],
    [
      "@semantic-release/npm",
      {
        "npmPublish": true
      }
    ],
    [
      "@semantic-release/exec",
      {
        "prepareCmd": "jq --arg v '${nextRelease.version}' '.version = $v | .packages[0].version = $v' server.json > server.json.tmp && mv server.json.tmp server.json"
      }
    ],
    [
      "@semantic-release/git",
      {
        "assets": ["CHANGELOG.md", "package.json", "package-lock.json", "server.json"],
        "message": "chore(release): ${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
      }
    ],
    "@semantic-release/github"
  ]
}
